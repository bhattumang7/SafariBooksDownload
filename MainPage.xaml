<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SafariBooksDownload.MainPage">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">
            <HorizontalStackLayout>
                <Entry x:Name="bookName" Text="design" Placeholder="Enter book name here"  />
                <Button x:Name="downloadbtn" Text="Click me!" Clicked="OnSearchButtonClick" />

            </HorizontalStackLayout>

            <!--<ListView ItemsSource="{Binding Books}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackLayout Orientation="Horizontal">
                            --><!--<Image Source="{Binding cover_image}" HeightRequest="100" WidthRequest="100" />--><!--
                            <StackLayout Orientation="Vertical" Spacing="5">
                                <Label Text="{Binding title}" FontSize="Large" />
                                <Label Text="{Binding product_id}" FontSize="Small" />
                                <Label Text="{Binding authors}" FontSize="Small" />
                            </StackLayout>
                        </StackLayout>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>-->



            <WebView x:Name="AuthWebView" HeightRequest="600" Source="https://learning.oreilly.com/profile"
Navigated="AuthWebView_OnNavigated" />

            <ListView ItemsSource="{Binding Books}">
                <ListView.RowHeight>250</ListView.RowHeight>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <StackLayout x:Name="itemLayout" Orientation="Horizontal" WidthRequest="400" HeightRequest="250" VerticalOptions="Fill" HorizontalOptions="Fill">
                                <Image Source="{Binding cover_image}" HeightRequest="100" WidthRequest="100" />
                                <StackLayout Orientation="Vertical" Spacing="5" VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand">
                                    <Label Text="{Binding title}" FontSize="Large" LineBreakMode="WordWrap" />
                                    <Label Text="{Binding authors}" FontSize="Small"   />
                                    <Label Text="{Binding product_id}" FontSize="Small"  />
                                </StackLayout>
                                <Button Text="Download" Clicked="downloadBook" CommandParameter="{Binding .}" />
                            </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>





            <Image
                Source="dotnet_bot.png"
                HeightRequest="185"
                Aspect="AspectFit"
                SemanticProperties.Description="dot net bot in a race car number eight" />

            <Label
                Text="Hello, World!"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />

            <Label
                Text="Welcome to &#10;.NET Multi-platform App UI"
                Style="{StaticResource SubHeadline}"
                SemanticProperties.HeadingLevel="Level2"
                SemanticProperties.Description="Welcome to dot net Multi platform App U I" />

            <Button
                x:Name="CounterBtn"
                Text="Click me" 
                SemanticProperties.Hint="Counts the number of times you click"
                Clicked="OnCounterClicked"
                HorizontalOptions="Fill" />
            
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
